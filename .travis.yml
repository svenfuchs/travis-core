language: ruby
services:
  - redis
rvm:
  - 1.9.3
before_script:
  - 'RAILS_ENV=test rake db:create db:schema:load --trace'
script:
  - './build.sh'
notifications:
  irc: "irc.freenode.org#travis"
after_failure: "cat /var/log/redis/redis.log"
